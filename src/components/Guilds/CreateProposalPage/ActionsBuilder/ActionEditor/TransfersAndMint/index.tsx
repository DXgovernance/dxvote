import styled from 'styled-components';
import { CardWrapper } from 'components/Guilds/common/Card';

const CardWrapperWithMargin = styled(CardWrapper)`
  margin-top: 0.8rem;
`;

const TransfersAndMint: React.FC = () => {
  return <CardWrapperWithMargin>Test</CardWrapperWithMargin>;
};

export default TransfersAndMint;
