import { ActionEditorProps } from '..';

const Permissions: React.FC<ActionEditorProps> = ({
  decodedCall,
  updateCall,
}) => {
  return <div>Permissions div</div>;
};

export default Permissions;
